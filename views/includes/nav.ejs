<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/"><span style="color: rgb(72, 182, 72);">Goals</span> Tracker</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup"
      aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <% if(!isAuthenticated) { %>
        <a class="nav-item nav-link" href="/auth/login">Login</a>
        <a class="nav-item nav-link" href="/auth/signup">Signup</a>
        <% } else {%>
        <a class="nav-item nav-link" href="/goals/dashboard">DASHBOARD</a>
        <a class="nav-item nav-link" href="/account/profile">PROFILE</a>
        <form action="/auth/logout" method="POST">
          <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
          <button type="submit">LOGOUT</button>
        </form>
        <% } %>
      </div>
    </div>
  </div>
</nav>